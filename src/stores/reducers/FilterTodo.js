const initialState = [];
const filterTodo = (state = initialState, action) => {
  //   switch (action.type) {
  //     case "addTodo":
  //       return [...state, action.payload.name];
  //     default:
  //       return state;
  //   }
  return [];
};

export { filterTodo };
