const initialState = [];
const listTodo = (state = initialState, action) => {
  //   switch (action.type) {
  //     case "addTodo":
  //       return [...state, action.payload.name];
  //     default:
  //       return state;
  //   }
  return [];
};

export { listTodo };
